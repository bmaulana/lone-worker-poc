<Page
    x:Class="testWP8App.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:testWP8App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel x:Name="contentPanel" Margin="10,10,0,0">
            
            <Button x:ConnectionId='1' x:Name="bandButton" Content="Fire up Band sensors" Margin="0,0,0,0"                  />
            
            <StackPanel x:Name="heartRatePanel" Orientation="Horizontal" Margin="0,20,0,0">
                <TextBlock Text="Your heart rate is:" FontSize="16" HorizontalAlignment="Left"/>
                <TextBlock x:Name="heartRateOutput" Margin="20,0,0,0" FontSize="16"/>
            </StackPanel>

            <StackPanel x:Name="tempPanel" Orientation="Horizontal" Margin="0,20,0,20">
                <TextBlock Text="Your skin temperature is:" FontSize="16" HorizontalAlignment="Left"/>
                <TextBlock x:Name="tempOutput" Margin="20,0,0,0" FontSize="16"/>
            </StackPanel>

            <TextBox x:Name="titleInput" Width="300" HorizontalAlignment="Left" Text="" PlaceholderText="Title"/>
            <TextBox x:Name="bodyInput" Width="300" HorizontalAlignment="Left" Text="" PlaceholderText="Body"/>
            <Button x:ConnectionId='2' x:Name="notifButton" Content="Send notification to Band" Margin="0,0,0,0"                   />

            <TextBlock x:Name="bandOutput" Margin="0,0,0,0" FontSize="16"/>

            <Button x:ConnectionId='3' x:Name="toggleButton" Content="Start work" Margin="0,20,0,0"                    />

            <StackPanel x:Name="stepsPanel" Orientation="Horizontal" Margin="0,10,0,0">
                <TextBlock Text="Steps taken:" FontSize="16" HorizontalAlignment="Left"/>
                <TextBlock x:Name="stepsOutput" Text="Not started" Margin="20,0,0,0" FontSize="16"/>
            </StackPanel>

            <StackPanel x:Name="distancePanel" Orientation="Horizontal" Margin="0,10,0,0">
                <TextBlock Text="Distance walked:" FontSize="16" HorizontalAlignment="Left"/>
                <TextBlock x:Name="distanceOutput" Text="Not started" Margin="20,0,0,0" FontSize="16"/>
            </StackPanel>

            <StackPanel x:Name="timePanel" Orientation="Horizontal" Margin="0,10,0,0">
                <TextBlock Text="Time elasped:" FontSize="16" HorizontalAlignment="Left"/>
                <TextBlock x:Name="timeOutput" Text="Not started" Margin="20,0,0,0" FontSize="16"/>
            </StackPanel>

            <Button x:ConnectionId='4' x:Name="refreshButton" Content="Refresh" Margin="0,0,0,0"                     />

        </StackPanel>
    </Grid>
</Page>

